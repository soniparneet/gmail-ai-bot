# Changelog

All notable changes to this project will be documented in this file.

## [0.1.0] - 2026-04-12

### Added

- Initialized repository metadata and canonical production structure.
- Added `README.md`, `CHANGELOG.md`, `requirements.txt`, and `.env.example`.
- Added placeholder canonical directories under `src/`, `config/`, `utils/`, and `tests/`.
- Added classifier unit tests for LOW_VALUE normalization and signal extraction.
- Initialized local git repository workflow with `main` and `dev` branches.

### Preserved

- Existing `execution/` and `directives/` runtime layout to honor prior project-specific constraints.
